.env
# This file is used to set environment variables for the application.
DATABASE_URL=your_database_url
JWT_SECRET=your_jwt_secret  # Ensure to replace 'your_database_url' and 'your_jwt_secret' with actual values.
PORT=3000  # Default port for the application, can be changed as needed.                                                    
# Additional environment variables can be added as needed.
# Note: Do not commit this file to version control if it contains sensitive information.    
# Example of how to use these variables in your application:
# import dotenv from 'dotenv';  
# dotenv.config();
# const dbUrl = process.env.DATABASE_URL;   
# const jwtSecret = process.env.JWT_SECRET;
# const port = process.env.PORT || 3000;  # Use the PORT variable or default to 3000 if not set.
# Make sure to keep this file secure and not expose it in public repositories.
# Ensure to add this file to your .gitignore to prevent it from being committed to version control.
# Example .gitignore entry:
# .env      
# This file is used to set environment variables for the application.
# It is recommended to keep this file out of version control to protect sensitive information.  
# You can use the dotenv package to load these variables into your application.
VITE_TMDB_API_KEY=01e30928a26fc1f5baeba16656888feb  # Replace with your actual TMDB API key.
# This key is used to access the TMDB API for fetching movie data.      
